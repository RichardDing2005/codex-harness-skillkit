# Legacy example

This example exists to exercise the retrofit inventory workflow.

## Project Structure

- `analysis.py` — legacy workflow entry point
- `notes.txt` — legacy notes kept alongside the workflow
