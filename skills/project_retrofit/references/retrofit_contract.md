# Retrofit contract

`project_retrofit` applies when the repository already contains meaningful material that must be preserved.

The skill must inventory existing material before applying any overlay. It should prefer migration mapping and preservation constraints over immediate file changes.
