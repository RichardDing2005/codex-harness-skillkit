# Evolution contract

`project_evolution` applies only when the repository already has a valid harness kernel.

The skill should create an impact report before high-impact changes and must preserve state and memory continuity when runtime truth changes.
