# Codex usage

The canonical end-user usage guide for this package is [`docs/USE.md`](./USE.md).

Use that file as the single source of truth for:

- discovery checks
- backup recommendations
- explicit invocation examples
- `project_retrofit` planning and `apply-safe-overlay`
- `project_evolution` entry guidance

This file is intentionally short to avoid drift across duplicate usage documents.
